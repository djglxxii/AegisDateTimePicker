/**
 * Entry point for aegis-datetime-picker custom element
 * Importing the Svelte component registers it as a custom element
 */
import './lib/AegisDateTimePicker.svelte';

// Export for module consumers
export { default as AegisDateTimePicker } from './lib/AegisDateTimePicker.svelte';
